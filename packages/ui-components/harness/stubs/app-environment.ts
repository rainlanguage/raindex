// Stub for SvelteKit's `$app/environment`. The harness always runs in a real
// browser tab.
export const browser = true;
export const dev = true;
export const building = false;
export const version = 'harness';
